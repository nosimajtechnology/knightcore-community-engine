# Knightcore Community Engine compatibility entry point

The canonical installable Skill is [skill/knightcore-community-engine/SKILL.md](skill/knightcore-community-engine/SKILL.md).

This repository-level file is a compatibility pointer only. It is excluded from the release ZIP so the package contains exactly one manifest.

